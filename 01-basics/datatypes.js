"use strict"; // Treat all js code as newer version

console.log(3 + 3);
console.log("Shyam");

let fname = "Shyam";
let age = 17; 
let isLoggedIn = true;
let state = null;
let address; // Returns undefined. 

// Number, bigInt, string, boolean, null, undefined, symbol, object, array

console.log(typeof fname);
console.log(typeof age);
console.log(typeof undefined); // Returns undefined
console.log(typeof null); // Returns object. 


   