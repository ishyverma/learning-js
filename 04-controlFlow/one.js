// If

if (true) {
    // Will execute the code in this scope. 
}
if (false) {
    // Will not execute the code in this scope.
}

// Implicit scope
const balance = 100;
if(balance > 50) console.log("test");